server:
  port: 2333
  address: 0.0.0.0

lavalink:
  server:
    password: "youshallnotpass"
    sources:
      youtube: false  # Disable built-in, use plugin instead
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      http: true
      local: false
      spotify: false  # No Spotify plugin installed

plugins:
  youtube:
    enabled: true
    allowSearch: true
    allowDirectVideoIdAccess: true
    allowDirectPlaylistIdAccess: true
    clients:
      - ANDROID_TESTSUITE
      - WEB
      - TVHTML5EMBEDDED
    oauth:
      enabled: false

logging:
  level:
    root: INFO
    lavalink: INFO
    youtube: DEBUG
