server:
  port: 2333
  address: 0.0.0.0
lavalink:
  server:
    password: "youshallnotpass"
    sources:
      youtube: true  # Keep YouTube source enabled (deprecated but works for now)
      spotify: false  # Disable - using lavasrc plugin instead
plugins:
  lavasrc:
    sources:
      spotify: true
      youtube: true  # Enable YouTube in lavasrc (requires YouTube source plugin)
    spotify:
      clientId: "${SPOTIFY_CLIENT_ID}"
      clientSecret: "${SPOTIFY_CLIENT_SECRET}"
      countryCode: "US"
logging:
  level:
    root: INFO
    lavalink: DEBUG
    lavalink.server.io.SocketContext: TRACE